# Production Environment Template for Render.com
# Copy this to .env in production and fill with actual values

# ✅ REQUIRED: Generate strong secrets
JWT_SECRET=ZMIEŃ-NA-BARDZO-DŁUGIE-LOSOWE-HASŁO-256-BITÓW
COOKIE_SECRET=INNE-BARDZO-DŁUGIE-LOSOWE-HASŁO-256-BITÓW

# ✅ REQUIRED: Update CORS for production domains
STORE_CORS=https://falkoproject.com,https://twoja-domena.vercel.app
ADMIN_CORS=https://admin.falkoproject.com,https://medusa-admin.onrender.com
AUTH_CORS=https://falkoproject.com,https://admin.falkoproject.com

# ✅ Database - use your production database with SSL
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require
DB_NAME=medusa-v2

# ✅ Optional: Add monitoring
SENTRY_DSN=https://twoj-sentry-dsn@sentry.io/project-id

# ✅ Optional: Email service (for order confirmations)
SENDGRID_API_KEY=SG.twoj-sendgrid-key
FROM_EMAIL=orders@falkoproject.com

# ✅ Node Environment
NODE_ENV=production

# ✅ Furgonetka (Production)
# Switch base URL to production API and provide OAuth credentials
FURGONETKA_BASE_URL=https://api.furgonetka.pl
FURGONETKA_OAUTH_CLIENT_ID=
FURGONETKA_OAUTH_CLIENT_SECRET=
# Optional OAuth params (used if your tenant requires them)
FURGONETKA_OAUTH_SCOPE=
FURGONETKA_OAUTH_AUDIENCE=
# Optional: tokens for securing inbound callbacks/webhooks
FURGONETKA_WEBHOOK_TOKEN=
FURGONETKA_AUTH_TOKEN=
FURGONETKA_PARTNER_USER_ID=
FURGONETKA_COMPANY_ID=
FURGONETKA_USER_EMAIL=

# ✅ Store sender details for shipping labels
STORE_NAME=
STORE_ADDRESS=
STORE_CITY=
STORE_POSTCODE=
STORE_PHONE=
STORE_EMAIL=
